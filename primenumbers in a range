low= int(input())
up = int(input())
a =set()
count =0
for num in range(low, up+1):
    if (num > 1):
        for i in range(2,num):
            if( num % i )== 0:
                break
        else:
            a.add(num)
            count +=1

print(a)
print(count)
