if __name__ == '__main__':
    n = int(input())
    arr = map(int, input().split())
    lst = list(arr)
    lst.sort(reverse=True)
    for i in range(len(lst)):
        if lst[i]!=lst[i+1]:
            break
        else:
            continue
    
    print(lst[i+1])
